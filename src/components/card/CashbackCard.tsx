import { Box, styled } from '@mui/material';

export const CashbackCard = () => {
  return <CashbackCardContainer></CashbackCardContainer>;
};

const CashbackCardContainer = styled(Box)(({ theme }) => ({
  borderRadius: '15px',
}));
